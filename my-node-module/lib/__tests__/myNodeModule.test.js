const assert = require('assert');
const myNodeModule = require('../index.js');

describe('myNodeModule', () => {
  it('has a test', () => {
    assert(false, 'myNodeModule should have a test');
  });
});
